# Build / Verification — Main App 0.5.29

- Version: 0.5.29 / versionCode 52.
- `CameraRecordingStatusRegistry.kt` compiled successfully with the local Kotlin compiler against focused Android `Environment` / `FileObserver` stubs.
- Per-video recording lifecycle harness: PASS.
  - Camera start + MP4 CREATE => Recording.
  - MP4 CLOSE_WRITE does not drop Recording.
  - IMU close does not drop Recording.
  - A newer video start supersedes an older still-latched video.
  - Delayed `addFile` for the previous video does not stop the new video.
  - Matching `addFile` stops the current recording and records a recent-completion hint.
  - `.part` / `.tmp` in-progress aliases match the finalized `.mp4` / `.sti` completion identity.
  - The status-only FileObserver now forwards temporary writer aliases to the live recording registry, while the processing engine still receives only finalized `.mp4` candidates.
  - Processing protection normalizes `.part` / `.tmp` aliases to the finalized video identity, so a rename cannot make the still-active Camera file eligible for processing.
  - Late MODIFY events after matching `addFile` cannot resurrect Recording.
- `OutputMover.kt` compiled successfully against focused Android/project API stubs.
- Local OutputMover integration harness: PASS on a 12 MiB MP4-like file; destination contents matched, destination finalized, and source cleanup completed.
- Static verification: production `OutputMover.kt` contains no SHA-256 / `MessageDigest` full-file verification path.
- Completed-video acceleration is explicitly 2,000 ms with a 2,100 ms follow-up scan; generic fallback remains 30,000 ms.
- Completed transaction commit explicitly calls `ensureReportFiles(reportDestination)` before report append.
- Full Gradle `testDebugUnitTest assembleDebug` could not run in this sandbox because Gradle 7.6.4 is not cached and the Android SDK / outbound dependency download path is unavailable. The included GitHub workflow remains the final full Android build check.
